<div
  class="markdown-content prose prose-stone max-w-none lg:prose-lg"
>
  <slot />
</div>

<style>
  .markdown-content {
    --tw-prose-body: hsl(20 8% 21%);
    --tw-prose-headings: hsl(var(--foreground));
    --tw-prose-lead: hsl(var(--muted-foreground));
    --tw-prose-links: hsl(var(--brand));
    --tw-prose-bold: hsl(var(--foreground));
    --tw-prose-counters: hsl(var(--muted-foreground));
    --tw-prose-bullets: hsl(var(--brand));
    --tw-prose-hr: hsl(var(--border));
    --tw-prose-quotes: hsl(20 7% 31%);
    --tw-prose-quote-borders: hsl(var(--brand));
    --tw-prose-captions: hsl(var(--muted-foreground));
    --tw-prose-code: hsl(343 42% 28%);
    --tw-prose-pre-code: hsl(20 10% 20%);
    --tw-prose-pre-bg: hsl(28 26% 95%);
    --tw-prose-th-borders: hsl(var(--border));
    --tw-prose-td-borders: hsl(var(--border));

    max-width: 46rem;
    color: var(--tw-prose-body);
    font-family: var(--font-body);
  }

  .markdown-content :global(h1) {
    border-top: 1px solid hsl(var(--border));
    color: hsl(343 54% 25%);
    padding-top: 0.7em;
  }

  .markdown-content :global(h1:first-child) {
    border-top: 0;
    padding-top: 0;
  }

  .markdown-content :global(a) {
    text-decoration-thickness: 1px;
    text-underline-offset: 0.16em;
  }

  .markdown-content :global(a:hover) {
    color: hsl(343 76% 27%);
  }

  .markdown-content :global(blockquote) {
    border-left-width: 0.2rem;
    background: color-mix(in srgb, hsl(var(--accent)) 52%, transparent);
    padding: 0.75em 1em;
    font-family: var(--font-display);
    font-size: 1.08em;
  }

  .markdown-content :global(img),
  .markdown-content :global(video) {
    border-radius: 0.5rem;
  }

  .markdown-content :global(figure img),
  .markdown-content :global(a img),
  .markdown-content :global(td img) {
    margin: 0;
  }

  .markdown-content :global(.mdx-image) {
    margin: 2rem 0;
  }

  .markdown-content :global(.markdown-side-by-side__media .mdx-image) {
    margin: 0;
  }

  .markdown-content :global(.markdown-side-by-side__content > :first-child) {
    margin-top: 0;
  }

  .markdown-content :global(:not(pre) > code) {
    border: 1px solid hsl(var(--border));
    border-radius: 0.25rem;
    background: hsl(28 24% 94%);
    padding: 0.08em 0.32em;
  }

  .markdown-content :global(pre) {
    border: 1px solid hsl(var(--border));
    border-radius: 0.625rem;
    background: hsl(28 26% 95%) !important;
    box-shadow: inset 0 1px 0 rgb(255 255 255 / 72%);
  }

  .markdown-content :global(code),
  .markdown-content :global(code *),
  .markdown-content :global(pre),
  .markdown-content :global(pre *) {
    font-family: var(--font-mono) !important;
  }

  .markdown-content :global(pre code) {
    border: 0;
    background: transparent;
    padding: 0;
    font-weight: 400;
  }

  .markdown-content :global(details) {
    border: 1px solid hsl(var(--border));
    border-radius: 0.5rem;
    background: color-mix(in srgb, hsl(var(--background)) 76%, white);
    padding: 0.8rem 1rem;
  }

  .markdown-content :global(summary) {
    cursor: pointer;
    color: hsl(343 54% 25%);
    font-weight: 700;
  }
</style>
